# Former location: `docs/driver-handlers/`

Per-device topic READMEs that used to live here were **removed**. Handler APIs and behaviour live in **`lib/core/handlers/<device>/`** (hf-core submodule) and on [hf-core GitHub Pages](https://hardfoc.github.io/hf-core/).

**Use instead:** [`../hal/managers-and-handlers.md`](../hal/managers-and-handlers.md) (which manager composes which handler) and the handler headers under `lib/core/handlers/`.
