# Former location: `docs/component-handlers/`

Per-manager topic READMEs that used to live here were **removed** — they duplicated and often contradicted `lib/managers/*.h` and `Vortex.cpp`.

**Use instead:** [`../hal/README.md`](../hal/README.md) and [`../hal/managers-and-handlers.md`](../hal/managers-and-handlers.md).

If you followed an old link to a `*_MANAGER_README.md` file in this folder, update bookmarks to the hub above or to the specific header under `lib/managers/`.
