# ===========================================================================
# .gitignore — ESP-IDF example project build artifacts
# ===========================================================================

# ── Build output directories ──────────────────────────────────────────────
# All build artifacts generated by build_app.sh / idf.py
builds/
build/

# ── ESP-IDF generated files ──────────────────────────────────────────────
# sdkconfig is auto-generated from sdkconfig.defaults + menuconfig
# Only sdkconfig.defaults should be committed
sdkconfig
sdkconfig.old

# ESP-IDF managed components & dependencies lock
managed_components/
dependencies.lock

# ── Python / ESP-IDF tooling ──────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
